{{ printf "{{.AppName}}" }} - {{ .Short }}
  Version Installed:    {{ .Version }}
  Newest Available:     {{ latestVersion }}
{{ if (canUpdate) -}}
  Visit {{ releaseURL }} to get the latest version
{{- end -}}
